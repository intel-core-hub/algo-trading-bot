# algo-trading-bot 実行計画書 — 研究完了→実行検証フェーズへ

**前提**: 本プロジェクトは「価格の方向を当てる」系のアプローチは全滅、方向性を予測しない
ファンディング・キャリー(現物ロング+無期限先物ショート)のみがBTC/ETHで5.5年・train/test
全パターンで頑健にプラス(Sharpe 7〜9.5、最大DD1%未満)という結論に到達済み。
コードは4回の外部レビューを経て、原子性・状態照合・OS互換性・自動テストが一通り整備済み。

**残っている問題は「リサーチ」ではなく「実行検証がゼロ」であること**。
テストネットAPIキーを一度も取得しておらず、実発注コードは一行も実行されたことがない。
本計画書はこのギャップを埋めるための優先順位付きタスクである。

**【2026-09-11 追記・重大な制約が判明】**: 日本居住者がFutures(先物)を実行できる経路を
調査した結果、当初は「戦略の経済的前提が成立し、かつプログラムから実発注できる」候補が
一つも見つからなかった。

| 候補 | 経済的な仕組み(funding income相当) | API |
|---|---|---|
| Binance Japan | 先物・デリバティブ自体が個人向けに提供されていない | — |
| Bybit等海外勢 | 金融庁の警告を経て日本居住者向けサービスを終了中 | — |
| GMOコイン/DMM Bitcoin | 一律の保有手数料(0.04%/日、年率14.6%相当)を払うのみ。
funding income相当の収益機会が存在しない | あり |
| SBI VCトレード | 双方向のファンディングレート、戦略の前提と合致 | 未公開(公式回答: 今後公開予定、時期未定) |
| **bitFlyer Crypto CFD** | **双方向のファンディングレート(符号付き、Binanceのperpetual
funding rateと同型)。2024年3月にLightning FXから移行した際に導入** | **利用可能(REST+Realtime、
旧Lightning FX APIと互換)** |

**【2026-09-11 再追記・訂正】**: bitFlyer Crypto CFDが有力候補として見つかった。
当初「JPの取引所は一律の保有手数料型」と結論づけたのは、2024年3月に廃止された旧
「Lightning FX」の情報に基づく誤りで、後継のbitFlyer Crypto CFDは仕組みが変わっている。
公式APIドキュメントに`current_funding_rate`(符号付き、プラス/マイナス両方)を返す
「Funding Rate」APIが追加されており、Binanceのperpetual futures funding rateと構造的に
同じ。APIは現時点で実際に利用可能(SBIのような「今後公開予定」ではない)。レバレッジは
2倍上限(国内法規制、デルタニュートラル戦略には問題なし)。現物取引も同一プラットフォームで
扱えるため、スポット脚とCFD脚を同一取引所内で完結できる可能性がある。

次のステップ: bitFlyer Crypto CFDの過去のfunding rate推移データを取得し、Binanceの
検証と同様に実際の収益機会(水準・安定性)を再検証する。Binance固有の市場構造に依存した
結果だった可能性があるため、これは新規のバックテストとして扱う。

**【2026-09-11 さらに追記】**: `GET /v1/getfundingratehistory`(product_code=FX_BTC_JPY)は
**Public API**(認証不要、口座もKYCも不要)。つまりこの再検証はユーザーの作業を待たずに
Claude Codeだけで今すぐ着手できる。KYC・口座開設が必要になるのは、再検証で経済的に
成立するという結果が出た後、実際に少額発注をテストする段階から。

Binance Futures Testnetでの実行検証(フェーズB原案)は、テストネット自体は技術的に
可能でも、日本居住者アカウントはBinance Japanへ強制的にルーティングされ、そもそも
先物メニューに到達できない(KYC完了の有無に関係ない構造的な制約)。この問題自体は
残るが、bitFlyer Crypto CFDという代替の実行先候補が見つかったことで、フェーズE
(実弾投入)の実現可能性は再び開けた状態になった。

**【2026-09-11 再検証・結論: bitFlyer Crypto CFDは現時点で経済的に成立しない】**:
`GET /v1/getfundingratehistory`(Public API、認証不要)で取得できる全履歴
(2024-03-28〜2026-09-11、2690件)を使い、`backtests/check_bitflyer_funding_viability.py`
として再現可能な形で検証した。結果:

- funding rateだけを見ると年率換算9.7%(全期間平均)で、一見Binanceの検証結果と
  近い水準に見える。しかしtrain(前半70%)は+14.4%、test(直近30%)は**-1.2%**と、
  Binanceで確認された「train+10%→test+3〜4%」より急激に縮小しており、直近4四半期
  (2025Q4〜2026Q3)は年率換算で-0.2%〜-3.2%とほぼ全てマイナス。直近7日トレーリング
  平均(`decide_carry_action`が実際に見る値)は年率+0.8%まで低下している。
- **さらに重要な見落とし**: bitFlyer Crypto CFDには`funding rate`とは別建てで
  「レバレッジポイント」という日次コストがあり、買建玉・売建玉を問わず建玉金額の
  **0.04%/日(年率換算約14.6%)**を徴収される(公式手数料ページで確認)。funding rateは
  買い/売り間のゼロサムの授受だが、レバレッジポイントはbitFlyerへの純粋な支払いなので、
  キャリー戦略のCFDショート脚には両方が重なる。
- この14.6%/年のコストを差し引くと、**全期間平均は-4.9%/年、直近4四半期は
  -13.8%〜-17.8%/年、直近7日トレーリングの現在値も-13.8%/年**となり、2024年前半の
  一部の四半期(2024Q2, 2024Q4)を除いてほぼ全期間でマイナス。現時点でこの戦略を
  bitFlyer Crypto CFDで実行しても、経済的な優位性は無い。

**結論**: bitFlyer Crypto CFDはAPIが利用可能という点では有力だったが、コスト構造
(レバレッジポイント)を含めて再検証した結果、現時点では実行先として推奨できない。
フェーズEはBinanceの結果ともども保留とし、フェーズBはコード検証(モック)のみを
目的として引き続き進める。

**ガードレール(Claude Codeが単独で越えてはいけない境界)**:
- APIキーの発行・`.env`への実際の値の記入・秘密鍵の取り扱いは、常にユーザー本人が行う。
  Claude Codeは`.env`の**フォーマット**や読み込みコードを整備できるが、実際の鍵の値を
  代わりに入力・生成することはしない。
- 実際の資金(テストネットの仮想資金を除く)を動かす操作は、本計画のどのフェーズでも
  Claude Codeの担当外。フェーズEに到達しても、発注ボタンを押すのはユーザー本人。
- 各フェーズの完了時、次フェーズに進む前に一度ユーザーに結果を報告し、続行の意思を確認する。
  特にフェーズC→D、フェーズD→Eの移行は必ず確認を挟む(判断が伴うため)。

---

## フェーズA: テストネット環境構築(前提整備)

- [ ] ユーザーがBinance Spot Testnet / Futures Testnetのアカウントを作成し、APIキーを発行
      (Claude Codeの作業ではない。ユーザー本人が行い、キーの値だけを`.env`に貼り付ける)
- [x] `.env.example`を新規作成し、必要な環境変数名(4つ、値は空欄)を明記
      (`chore/testnet-env-setup`, commit `e4b6ada`)
- [x] `.env`読み込み時、キー未設定なら分かりやすい`RuntimeError`で落ちる実装は既にあったが、
      それを保証する回帰テストが無かったため`tests/test_execution.py`に追加(spot/futures両方、
      `monkeypatch.delenv`で検証)。`pytest`53件全パス(既存51+新規2)
- [x] README.mdセットアップ節に`.env.example`の使い方を追記
- [x] ブランチはmasterへマージ済み想定(フェーズBとは独立した変更のため、キー待ちで
      寝かせず先にマージする方針とした)

## フェーズB: 実発注ロジックの検証(実口座なしで進める版)

これまでの3〜5回目の外部レビューで直した項目(2レッグ発注の原子性、state reconciliation、
クールダウン、OS非依存ロック)は**全てバックテスト/単体テストレベルの検証**であり、
実際のAPI応答に対して動かしたことは一度もない。当初はBinance Futures Testnetで
検証する想定だったが、日本居住者は先物メニュー自体に到達できないため、実口座を
使わない形に組み替える。Spot側(testnet.binance.vision)はGitHubログインのみで
利用できているため、こちらは当初案通りテストネットで検証してよい。

- [ ] Futures側の応答を模したモックサーバー(またはモックオブジェクト)を自作し、
      成功・部分失敗(先物側だけ拒否)・タイムアウト・あいまいな失敗(API例外だが実際は
      約定済み)などのシナリオを再現できるようにする
- [ ] Spot側はtestnet.binance.visionの実APIに向けて、Futures側は上記モックに向けて
      `--live`を実行し、まず「何もしない」ことを確認(dry-run)
- [ ] dry-runを解除し、モック相手にcarryポジションを1回建てる(現物ロング→先物ショートの順)
- [ ] モック側で意図的に失敗を注入し、設計通り「現物のみロールバック」または
      `needs_manual_intervention`フラグが正しく立つことを確認
- [ ] `reconcile_state`が(Spotは実残高、Futuresはモックの想定残高との)食い違いを
      検知して止められるかを確認
- [ ] `--min-hold-days`のクールダウンが実際の日付をまたいで機能するか確認
- [ ] Windows環境での排他ロック(`O_CREAT | O_EXCL`)が想定通り動くか、同時実行を試して確認
- [ ] 上記すべてで見つかった不具合を修正し、`tests/`に回帰テストとして追加

**このフェーズはコードの正しさを検証するものであり、実口座問題の解決策ではない。**
**モックで検証が通っても、実弾で発注できる先が無ければフェーズEには進めない。**

## フェーズC: 実運用パラメータの再検証

- [ ] 実際に使う予定の取引所の手数料ティア(現物・先物それぞれ)を調べ、
      現状の前提(15〜30bps相当)を実データで置き換えて`run_funding_carry.py`を再実行
- [ ] 現物・先物ウォレット間のポートフォリオマージンの実際の仕様(その取引所固有)を調査。
      無い場合、経済的にヘッジ済みでも証拠金不足で追証が起きるリスクを再評価
- [ ] 直近の資金調達率の水準を再取得し、「train+10%→test+3〜4%」という縮小トレンドが
      現時点でどこまで進んでいるかを確認(エッジがまだ生きているかの生死判定)
- [ ] 上記を反映した最新のバックテスト結果をREADMEに追記

**→ ここでユーザーに一度中間報告。エッジがまだ生きているかで先に進む価値があるか変わる。**

## フェーズD: 拡張ペーパートレード

- [ ] テストネットで最低2〜4週間、`--live`を実際に稼働させ続ける
      (cronまたはタスクスケジューラで定期実行)
- [ ] 毎日または週次でログ(`state file`・注文履歴・エラー)を確認できる仕組みを用意
      (簡易な日次サマリーCSVやログのtail程度で十分、過剰な仕組みは不要)
- [ ] 想定通りの挙動(enter/exit頻度、クールダウンの効き方、エラー発生有無)になっているか確認

**→ ここでユーザーに結果を報告し、実弾投入に進むか判断してもらう。**

## フェーズE: 実弾投入 — 現時点で保留(bitFlyer Crypto CFDは再検証の結果、経済性NG)

- [x] bitFlyer Crypto CFD APIで`getfundingratehistory`の履歴を取得できるか確認
      → 2024-03-28〜現在、2690件取得できることを確認(Binanceの5.5年より短いが十分な量)
- [x] 取得したデータでBinance同様の再検証(train/test分割、Sharpe)を実施
      (`backtests/check_bitflyer_funding_viability.py`)
- [x] bitFlyer側の手数料体系をコスト前提に反映
      → 「レバレッジポイント」(0.04%/日、方向によらず)の存在が判明し、これを反映すると
      直近4四半期・現在値ともに年率-13%台のマイナスという結論になった(詳細は上記追記参照)
- [ ] **上記の結果、「経済的に成立する」とは言えないため、以下には進まない**:
  - [ ] 少額(失っても困らない額)から開始する方針を確認
  - [ ] 撤退基準(最大ドローダウン・連続失敗回数など、どうなったら止めるか)を事前に明文化
  - [ ] 実際の発注はユーザー本人が行う。Claude Codeはここでもコード面のサポートのみ

**保留中に様子を見る項目(急ぎではないが、状況が動いたら拾う)**:
- [ ] SBI VCトレードのAPI公開時期(公式FAQでは「今後公開予定」、時期未定)
- [ ] 同社BITPointサービス(VCTRADEとは別サービス)のAPI・funding rate相当の仕組みの有無
      (今回は未調査)
- [ ] bitFlyerのfunding rateが今後回復する可能性はあるため、
      `backtests/check_bitflyer_funding_viability.py`を定期的に再実行して監視する
      (レバレッジポイントのコストを上回るには、funding rateが年率15%程度を安定して
      上回る必要がある — 2024年前半以来その水準には戻っていない)

---

## 優先順位まとめ

1. ~~bitFlyer Crypto CFDのfunding rate再検証~~ → **完了、経済性NGという結論**
   (レバレッジポイント込みで直近は年率-13%台)。フェーズEはここで一旦行き止まり。
2. **フェーズB(モックでのコード検証)** — 実行先が未定でも、コードの正しさ自体は
   検証しておく価値があるため継続。
3. フェーズC・D・E — 新しい実行先候補(SBI VCトレードのAPI公開など)が出てくるか、
   bitFlyerのfunding rateが年率15%程度まで回復するまで保留。
