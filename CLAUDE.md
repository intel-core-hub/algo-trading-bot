# CLAUDE.md

このファイルは、このリポジトリでClaude Code(または他の開発者)が作業する際の
運用ルールをまとめたものです。プロジェクトの内容・スコープ・検証結果は
[README.md](README.md) を参照してください。

## Git運用ルール

### 1PR = 1目的

- 1つのPRには1つの目的だけを含める。バグ修正・新機能・依存関係の更新・
  ドキュメント整備などは目的ごとに別のPRに分ける。
- 「ついでに直した」変更が本来の目的と無関係な場合は、別PRに切り出す
  (レビューする側が「このPRは何をするものか」を一目で判断できる状態を保つため)。
- 例外: 外部レビューを取り込む一連の作業(下記)のように、性質上まとまった
  複数コミットになるのは構わないが、PR全体としての目的(例:
  「テストネット発注コードの安全性向上」)は1つに保つ。

### 外部レビュー(zip/patchなど)の取り込み方

過去に複数回、外部から`.zip`や`.patch`形式で修正案一式が提供され、取り込んだ
経緯がある(そのたびに、提案の一部が的確な指摘だった一方、提案コード自体にも
新たな不具合が見つかっている — 例: 実行環境がWindowsなのに`fcntl`を使うロック、
タイムスタンプの完全一致結合が実データの半分近くを捨てていた問題など)。
このため、外部レビューを取り込む際は必ず以下の手順を踏む。

1. **鵜呑みにせず、1件ずつ内容を読んで評価する。** レビューの指摘が正しいか、
   提案コード自体が正しいかは別問題として扱う。可能な限り自分で再現・
   検証してから採用する(例: 修正前のコードに一時的に戻してテストが
   実際に失敗することを確認してから、修正を復元する)。
2. **レビュー単位でコミットを分ける。** 1回のレビュー(1つのzip/patch)に
   対する対応は、他のレビューへの対応や無関係な変更と混ぜず、独立した
   コミット(必要なら複数コミット)にする。コミットメッセージには
   「何を採用したか」「何を採用しなかったか(と理由)」を具体的に書く。
3. **生のpatch/zipファイルや、それを適用するための一時スクリプト
   (`apply-*.py`など)はコミットしない。** 内容を確認・現在のコードへ
   手で移植したら、これらのファイルはリポジトリから削除する
   (`.gitignore`に`*.zip`・`*.patch`を含めており、誤コミットは防いでいるが、
   ワンオフの`apply-*.py`スクリプトのような命名も同様の性質を持つファイルは
   都度チェックして残さないこと)。
4. ベースが古い(現在のブランチの作業を反映していない)レビューは、
   そのまま`git apply`せず、現在のコードに対して差分の意図を再実装する。

### ブランチ命名規則

| プレフィックス | 用途 |
| --- | --- |
| `fix/<説明>` | バグ修正 |
| `feat/<説明>` | 新機能・新しい戦略/バックテストスクリプトの追加 |
| `docs/<説明>` | ドキュメントのみの変更 |
| `chore/<説明>` | 依存更新、CI設定、リファクタリングなど外部から見た挙動に影響しない変更 |

- `<説明>`はkebab-case、そのブランチの目的が一目で分かる短い文言にする
  (例: `fix/drawdown-stop-recovery`、`feat/funding-carry-strategy`)。
- 1ブランチに複数の目的を持たせない(1PR = 1目的の原則と対応)。

### コミットメッセージ

- タイトルは「何をしたか」を簡潔に、本文は**なぜ**その変更が必要だったかを書く
  (コードを読めば分かる「何を変えたか」の詳細説明は不要)。
- バグを修正した場合は、再現方法と検証方法(どうテストしたか)を書く。
- 末尾に `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>` を付ける。

### マージ前チェック

- マージする前に必ず `python -m pytest tests/` を実行し、全件パスすることを
  確認する。
- テスト件数などの自己申告(コミットメッセージやPR説明に書かれた「◯件パス」)は
  鵜呑みにせず、手元で再実行して同じ件数・結果になることを確認する。
- 安全性に関わる分岐(例: `src/execution.py`の発注失敗時のロールバック判断)を
  変更した場合は、意図した分岐に実際に入ることを、必要なら一時的に修正前へ
  戻すなどして確認してからマージする。
- マージの実行そのものは、明示的な指示がない限り行わない。
